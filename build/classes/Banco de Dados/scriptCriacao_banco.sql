/CREATE TABLE PACIENTE (
ID_PACIENTE INT IDENTITY(1,1) NOT NULL,
NOME VARCHAR(50) NOT NULL,
CPF VARCHAR(14) NOT NULL,
EMAIL VARCHAR(50),
DATA_NASC DATETIME NOT NULL,
SEXO VARCHAR(50) NOT NULL,
TELEFONE VARCHAR(50) NOT NULL
)

CREATE TABLE MEDICO (
ID_MEDICO INT IDENTITY(1,1) NOT NULL,
CRM VARCHAR(10) NOT NULL,
NOME VARCHAR(50) NOT NULL,
ESPECIALIDADE VARCHAR(50) NOT NULL,
EMAIL VARCHAR(50),
TELEFONE VARCHAR(50) NOT NULL
)

CREATE TABLE FUNCIONARIO (
ID_FUNC INT IDENTITY(1,1) NOT NULL,
NOME VARCHAR(50) NOT NULL,
CPF VARCHAR(14) NOT NULL,
DATA_ADMISSAO DATETIME NOT NULL,
EMAIL VARCHAR(50),
TELEFONE VARCHAR(50) NOT NULL,
SENHA VARCHAR(255) NOT NULL
)

ALTER TABLE PACIENTE
ADD CONSTRAINT PK_PACIENTE PRIMARY KEY (ID_PACIENTE);

ALTER TABLE MEDICO
ADD CONSTRAINT PK_MEDICO PRIMARY KEY (ID_MEDICO);

ALTER TABLE FUNCIONARIO
ADD CONSTRAINT PK_FUNCIONARIO PRIMARY KEY (ID_FUNC);

CREATE TABLE CONSULTA (
ID_CONSULTA INT IDENTITY(1,1) NOT NULL,
DATA_ATEND DATE NOT NULL,
HORARIO TIME NOT NULL,
ID_FUNC INT NOT NULL,
ID_PACIENTE INT NOT NULL,
ID_MEDICO INT NOT NULL,
)

ALTER TABLE CONSULTA
ADD CONSTRAINT PK_CONSULTA PRIMARY KEY (ID_CONSULTA);

ALTER TABLE CONSULTA
ADD CONSTRAINT FK_FUNCIONARIO_CONSULTA FOREIGN KEY (ID_FUNC) REFERENCES FUNCIONARIO (ID_FUNC);

ALTER TABLE CONSULTA
ADD CONSTRAINT FK_PACIENTE_CONSULTA FOREIGN KEY (ID_PACIENTE) REFERENCES PACIENTE (ID_PACIENTE);

ALTER TABLE CONSULTA
ADD CONSTRAINT FK_MEDICO_CONSULTA FOREIGN KEY (ID_MEDICO) REFERENCES MEDICO (ID_MEDICO);



